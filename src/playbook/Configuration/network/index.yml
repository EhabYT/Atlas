---
title: Network Domain
description: TCP, SMB, sharing and network hardening configuration
privilege: TrustedInstaller
actions:
  # =====================================================
  # EBOS Network domain
  #
  # Network-stack settings balancing latency,
  # throughput and security. Anonymous enumeration
  # is restricted; SMB bandwidth throttling is disabled.
  # =====================================================

  - !writeStatus: {status: 'Running network tweaks'}
  # Needed for compatibility
  # - !task: {path: 'network\disable-llmnr.yml'}
  - !task: {path: 'network\ebos-network-settings.yml'}

  # ---------- Shares / SMB ----------
  - !task: {path: 'network\shares\restrict-anonymous-access.yml'}
  - !task: {path: 'network\shares\restrict-anonymous-enumeration.yml'}
  - !task: {path: 'network\shares\disable-smb-bandwidth-throttling.yml'}
